services:
    verbier-bot:
        build: .
        container_name: verbier-whatsapp-bot
        restart: unless-stopped
        shm_size: '1gb'
        environment:
            - DATA_DIR=/app/data
        volumes:
            - ./data:/app/data
            - ./data:/app/.wwebjs_auth
        # For first-time setup, run: docker compose up
        # After auth, run detached: docker compose up -d
